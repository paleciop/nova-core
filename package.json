{
  "name": "nova-core",
  "version": "1.0.0",
  "description": "Small templating framework that decouples the presentation layer with the business logic. Based on XumaK's Layer X specification.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "nova",
    "core",
    "templating",
    "context",
    "processor"
  ],
  "author": "Palecio",
  "license": "UNLICENSED",
  "devDependencies": {
    "prettier": "^1.5.3"
  },
  "dependencies": {
    "fs-extra": "^4.0.0",
    "klaw": "^2.0.0"
  }
}
